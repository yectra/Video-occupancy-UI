class CameraSetup {
    entranceName: string;
    cameraPosition: string;
    videoSource: string;
  }

  class Rectangle {
    x: number;
    y: number;
    width: number;
    height: number;
  }

  

